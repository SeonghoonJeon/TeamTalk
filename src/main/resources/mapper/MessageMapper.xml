<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.chat_server.mapper.MessageMapper">

    <insert id="insertMessage">
        INSERT INTO message (message_detail, member_id, dept_id, room_id)
        VALUES (#{messageDetail}, #{memberId}, #{deptId}, #{roomId})
    </insert>

    <select id="selectMessagesByRoomId" resultType="map">
        SELECT *
        FROM message
        WHERE room_id = #{roomId}
        ORDER BY message_id ASC
    </select>

</mapper>